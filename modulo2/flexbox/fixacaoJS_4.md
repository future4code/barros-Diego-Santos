``` javascript 
const verNumeros = arrayDeNumeros.filter( n => n === numeroEscolhido)
  if(arrayDeNumeros.includes(numeroEscolhido)){
    return `O número ${numeroEscolhido} aparece ${verNumeros.length}x`
  } else { return "Número não encontrado" }
```  