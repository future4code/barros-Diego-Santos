

```javascript
const somarValores = () => {
   const salarioVendedor = 2000;
   const valorBonus = valorTotalVendas * (5 / 100)
   const somar = (salarioVendedor + valorBonus) + (100 * qtdeCarrosVendidos) 
   return somar
}
return somarValores()
```